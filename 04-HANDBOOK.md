# 04 操作手册

**这本册子按你的四个问题组织**，对上哪条看哪节，装完就能用：

| 你的处境 | 看哪一节 |
|---|---|
| "这会话上下文用掉一半了，怎么办" | [第一节](#一这会话上下文用掉一半了怎么办) |
| "我想有个地方记着我在干的所有活" | [第二节](#二我想记着我在干的所有活) |
| "我开了新会话，想接着上个会话干" | [第三节](#三我开了新会话想接着上个会话干) |
| "我想派个活给下一个会话去干" | [第四节](#四我想派个活给下一个会话去干) |

**先记一句**：你只有三个动作——**说事 / 问进展 / 拍板**。其余（要不要立案、案怎么命名、什么时候收口、活派给子代理还是新会话）模型自己判。你只需要主动喊两句话：**"收口"**（一批活干完或会话变慢变贵）和**"周检"**（一周一次）。

| 动作 | 你说什么 | 会发生什么 | 你要做的 |
|---|---|---|---|
| **说事** | "我要做 X"，或 `/ctx-kickoff` | 分诊 → 该立案的当场建案文件 → 回你三行摘要 | 纠偏目标（A 节）与未决项（D 节），然后回答唯一那个问题：**在这儿聊，还是派出去** |
| **问进展** | "现在什么情况""那个案到哪了"，或 `/ctx-status` | 读任务板 + 案文件，人话汇报 | 什么都不用做——**不用翻文件** |
| **拍板** | "选方案 2""这条否掉" | 决策当场写进案 C 节（含"为什么、谁拍"） | 什么都不用做，痕迹自动留 |

为什么要这么干、这些数字哪来的 → 02-METHOD 与 01-BACKGROUND；"我有疑问" → 05-FAQ。

---

## 一、"这会话上下文用掉一半了，怎么办"

### 1. 先查两个表（10 秒）

**水位**（当前会话已占多少上下文）：

| 水位 | 状态 | 动作 |
|---|---|---|
| <150k | 绿 | 随便干 |
| 150–200k | 黄 | 预约下一个自然断点收口换生 |
| >200k | 红 | 这一轮就收口；别再往里灌新材料 |
| 讨论 / 评审型会话 | —— | 更硬：不看水位，**一批活干完就换生** |

**温度**（离开多久回来）：

| 间隔 | 判定 | 回来第一句该说什么 |
|---|---|---|
| ≤15 min | 稳热 | 照常说 |
| 15–60 min | 碰运气 | 照常说（赌不赌都改变不了价钱） |
| >1 h | 必冷 | **直接说正事**——寒暄一样付全额税 |

**两个变量决定动作**：

| 状态在盘上？ | 水位 | 动作 |
|---|---|---|
| 是 | 高 | **不回去**，新会话 `ctx-takeover` 接手（唯一全免冷税的走法） |
| 是 | 低 | 随意，直接继续 |
| 否 | 低 | 回去直接干 |
| 否 | 高 | 回去**第一轮就收口**，然后换生 |

### 2. 动作：说一句"收口"

```
你 ▸ 收口。

Claude ▸ 收口完成。
         · 案文件：cases/C-07_导出重做.md
         · H 未落盘清单 6 条，最重的一条：异步方案的压测脚本还在本会话里没落盘
           （已补落盘到 bench/export_async.py）
         · 状态：方案已定异步 + 轮询，候你拍分片大小；两个实验排队中
         本会话已退役；后续请开新会话，用 ctx-takeover 接手 C-07。
```

看到"已退役"就**别再在这个窗口里说话了**——继续聊等于把刚省下的钱再付一遍。

### 3. 第二次以后的收口是"更新"不是重写

```
你 ▸ 这批跑完了，收口。

Claude ▸ 收口完成（更新模式，本案第 2 次）。
         · B 节整段重写成当前终态，旧版滚进 F 一行
         · C 新增 3 条决策、E 新增 2 行实验判定、H 重列为 4 条（上一版 6 条里 5 条已落盘、新增 3 条）
         · 案文件：cases/C-07_导出重做.md　状态：候拍（分片大小的两个候选在 D 节）
         本会话已退役。
```

规律：**B 换新、C/E/F 只追加、H 重列；案号不变、E 行不变、只有持笔换名。**

### 4. 不要做的三件事

- **不要为"清爽一点"而 compact**：压缩保上下文、丢细节（实测一次自动压缩丢 98.8%），你要保的是状态，那是收口干的活。
- **不要在走之前做"保温"**：离开免费，钱只在回来发第一条请求时产生。
- **不要在高水位会话里派子代理**：等它回来的冷税 = 你当前水位 × 2。

---

## 二、"我想记着我在干的所有活"

### 1. 开一个活：直接说事

```
你 ▸ 我想把导出功能重做一版，现在导出经常超时。

Claude ▸ 这事要讨论 + 多轮实验，按立案走。已建 cases/C-07_导出重做.md
         · 目标（我的理解）：把导出 P95 从 40s 压到 5s 以内，两周内出方案并跑通一个对照实验
         · 关键未决：走异步任务 + 轮询，还是流式直出？两条路成本差一个数量级
         · 路径：cases/C-07_导出重做.md
         在这儿聊，还是派出去？

你 ▸ 目标改成 P95 8 秒就行，超时率是主指标。在这儿聊。

Claude ▸ A 节已改（附一行"为什么改：超时率才是用户感知指标"）。本会话标题已设为
         "C-07 导出重做 - 设计"，持笔已署名。开聊——
```

分诊有三条出口：**立案**（要讨论、多轮实验、要裁决）/ **散活**（一次执行，板上一行 + 一个交货件）/ **顺手改**（直接干）。聊的过程中每拍一个决策，你会看到它顺手说一句"已写进 C 节"——**正因为写了，这个会话随时可以扔掉。**

### 2. 看现在有什么活：问进展

```
你 ▸ 现在什么情况？

Claude ▸ 板上 3 个案、2 个散活：
         · C-07 导出重做（实验中，持笔=本会话）：方案已定异步+轮询，E-02 压测在跑
           → 等你拍：分片大小 4MB / 16MB，建议 4MB（内存峰值低一半）
         · C-09 计费口径（候裁）：两个方案都跑完了，判定相反，候你裁
         · C-11 老代码考古（已收口）：结论已进沉淀区，无待办
         · 散活 2 件：日志清理（已交货）、依赖升级（未开工，挂在"稳定性"节点）
         全库待拍板 2 项，都在上面标了"等你拍"。
```

**你不用翻文件**：目标链、各案状态、待拍板项、散活，一次说完。

### 3. 拍板：一句话，痕迹自动留

```
你 ▸ 分片就 4MB。C-09 按方案 B 走，理由是口径要跟财务对齐。

Claude ▸ 两条都已写进 C 节（日期 | 结论 | 为什么 | 谁拍）：
         · C-07：分片 4MB（内存峰值低一半）
         · C-09：采用方案 B（口径与财务对齐；方案 A 记为"已否决"并留了否决理由）
```

**被否决的路线也是决策，照样留档**——否则半年后有人会把它重新提一遍。

### 4. 一周一次：周检

```
你 ▸ 周检。

Claude ▸ 扫了 7 个会话，2 个越线：
         · a1b2… 讨论会话：p50 水位 380k、重写占比 41% ⚠️ → 该 ctx-handoff 了。
           买断价估 ~0.9M 当量；不收口的话，每次隔夜回访再付 ~0.76M。
         · c3d4… 执行会话：compact 1 次、峰值 620k ⚠️ → 它当时该落盘换生，不是压缩。
         已收口的 e5f6… 本期新增请求 0 → 退役终验通过。
         G 节回填 1/2：C-07 配上了档案路径；C-09 时间戳对不上，仍留"(待回填)"。
```

**"对不上就留空"是对的**——填一个"看起来像"的路径比空着更坏。判据线：重写占比 <10%，超了唯一解法是收口换生。

---

## 三、"我开了新会话，想接着上个会话干"

### 1. 动作：一条命令

```
你 ▸ /ctx-takeover C-07

Claude ▸ 已接手 cases/C-07_导出重做.md，标题设为 "C-07 导出重做 - 设计"，持笔已改名。
         · 总目标：把导出做成不用人盯的功能（本案服务"稳定性"这条线）
         · 本案目标：导出超时率压到 1% 以下，P95 ≤8s，两周内出方案
         · 方案核心：异步任务 + 轮询，分片写盘，前端拿任务 ID 拉进度
         · 下一步：把分片大小定下来，然后发 E-02 压测
         等你抽查提问，通过后我继续。
```

复述是**四层**：总目标 → 本案目标 → 当前进展 → 下一步。它只读案文件，**不读任何旧会话转录**（省钱，也防它把死掉的分支当成现行方案）。

### 2. 抽查是必做步骤：3+1 菜谱（四道题，两分钟）

目的不是考它记性，是验**案文件够不够用**。

| # | 题型 | 问句模板 | 通过标准 |
|---|---|---|---|
| 1 | **C 节"为什么"** | "当初为什么否掉〈那条被否的路线〉？" | 说得出理由和拍板日期，注明出自 C 节 |
| 2 | **B 节状态** | "现在方案里〈某模块〉是怎么设计的？" | 复述得上终态，不翻旧账 |
| 3 | **D 节候拍** | "现在等我拍的是哪几件？各自的建议是什么？" | 条数对、每项带建议 |
| 4 | **负对照** ★ | 问一件**答案不在案文件里、只在 H 或 G 指的那个文件里**的事，例如"上次压测的第 3 组数据是多少？" | **先说"案里没有"**，再按指针定点取一次并注明来源；取不到就说取不到 |

**负对照是四道里最重要的一道**——它测的不是知识，是**会不会编**。三种结果：

- 说"案里没有" + 定点取到 → **满分**（这就是要的行为）。
- 说"案里没有" + 取不到 → **通过**（诚实边界，然后你补落盘）。
- 张口就答、说得还挺像 → **判负**：让它标出处，标不出就重来一遍接手；同时说明案文件的指针不够硬。

可选第 5 题：**你自己出一道记忆题**（只有你和上个会话知道的事）。它测的是"捕获"——上个会话该落盘却没落盘的东西，这道题会照出来。

### 3. 一案只有一支笔

不能同时开两个会话接同一个案：两边各改 B/C，后写的覆盖先写的，账就对不上了。接手时如果"持笔"栏已有别人，它会先警告。要并行就拆成两个案，或者拆成 E 行派给执行体（执行体只写自己那行 E，天然不冲突）。

---

## 四、"我想派个活给下一个会话去干"

### 1. 先选载体（问活的形态，核心变量=方向不确定性，不是时长）

| 活的形态 | 载体 |
|---|---|
| 有方向叉口（设计/探索，中途可能要负责人定向） | **新会话** + 自足任务书，书里显式标"负责人停等点" |
| 确定性实验（预注册闭合无候拍点 + 只读或只写实验区 + 简单一次性） | 本会话派**子代理**，只报结果 |
| N 臂并行、当场横向对比 | 开一次性**低水位编排会话**派 N 个子代理，比完落盘即收口 |
| 重而确定（长时间机械放量） | **新会话**，不设停等点，拉模式交货 |

**拿不准就新会话。** 子代理中途无法与任何人对话——任务书里留着候拍点的一律不得内联。并且：**高水位会话不派子代理**——等它回来的冷税 = 你当前水位 × 2。

### 2. 任务书要自足（六件，缺一件就会返工）

在案 E 节立一行，然后把任务书写成一个新会话不用问你任何问题就能开跑的文件：

| # | 要素 | 说明 |
|---|---|---|
| 1 | 目标一句话 | 这活要回答的问题，不是"做什么动作" |
| 2 | 相关方案段**抄进去** | 不给它案文件路径让它自己读（读了就不独立了，也贵） |
| 3 | **预注册判据** | 动手前写死：改前读数 / 预期读数 / 判负条件 |
| 4 | 输入与边界 | 能碰什么、不能碰什么、禁读什么 |
| 5 | 交货路径与形态 | 双层交货件：机读层 + 给人看层，落到哪个目录 |
| 6 | 过程纪律 | **边干边落盘、回复保持短**（防长跑中断把成果蒸发掉） |

### 3. 派完就走，回收只看两样

```
你 ▸ 把 E-02 压测派出去。

Claude ▸ 已在 C-07 的 E 节立行 E-02（问题：4MB 与 16MB 分片在 P95 与内存峰值上差多少），
         任务书落 _staging/E-02_压测任务书.md，判据已预注册（P95 与峰值内存两个读数 + 判负条件）。
         这活重而确定（长跑压测、判据已闭合），按新会话派：请开新窗口喂这份任务书。本会话不等它。
```

回收时**只读交货件的"给人看层"**，把判定写回案 B/C，不要回执行会话的上下文里翻。E 节的"对方案的影响"列必填——否则会出现"算了没人接"：实验跑完、数字出了、方案没动。

---

## 五、按工作形态的配方

02-METHOD 第 5 章回答"哪类工作需要哪层"；这一节把它落成可照抄的节奏。**判据与门限以 02 为准，此处只引用不复制。**每个配方四件套：识别特征 / 装哪层 / 典型节奏 / 反模式。

### 配方 0：最小用法（先学这个）

只记两句话，其余全部可以后学：会话肥了或一批活干完，说 **"收口"**；开新会话接着干，说 **"接手 C-NN"**。这两个动作就守住了铁律（状态在盘上，会话可抛弃）。分诊、派活、周检都是在此之上的增量。

### 配方 1：研究 / 探索型

| 项 | 内容 |
|---|---|
| 识别 | 干之前结论未知，"能不能成、往哪走"要试出来 |
| 装哪层 | 全三层 |
| 节奏 | 说事 → 立案 → 导师会话讨论（**边拍边写 C/B**）→ 实验挂 E 行、派执行体跑 → 异步回收只读交货件给人看层 → 批界收口换生 → 每周周检 |
| 反模式 | 在导师会话里亲自跑长实验（该派出去）；等收口才补写案（该当场写）；执行体读案全文（该抄任务书，见 02 B5） |

### 配方 2：功能开发型

| 项 | 内容 |
|---|---|
| 识别 | 结论已知只差做完；ticket / PR / 测试就是现成状态载体 |
| 装哪层 | 经济层；知识层按需 |
| 节奏 | 日常活走散活 / 顺手改，**不立案**。kit 只在三处出场：遗留代码考古（digest 下放）、跨需求架构决策（小案或决策记录）、超一天的排障（进入"假设→实验→排除"循环即立案） |
| 反模式 | 给每个需求立案——与 ticket 双写必漂移（02 B4） |

### 配方 3：批量生产型（迁移、批量重构、批量生成）

| 项 | 内容 |
|---|---|
| 识别 | 同一动作放量，单元确定、总量大 |
| 装哪层 | 经济层 + 编排层 |
| 节奏 | 低水位编排会话**只派不干** → 执行会话群各写各的台账行 → **分波 + 终点站抽验**（02 第 5 章核心纪律）→ 波间回编排会话对账 |
| 反模式 | 全部生成完再统一验证；编排会话自己动手改文件（水位一涨，派子代理与被唤醒都变贵） |

### 配方 4：运维 / 排障型

| 项 | 内容 |
|---|---|
| 识别 | 事件驱动，多数短平快 |
| 装哪层 | 经济层；偶发知识层 |
| 节奏 | 短会话直接干、用完即弃；追凶超过一天就地立案，结案进复盘 |
| 反模式 | 用一个常驻长会话"值班"——隔夜必冷，每次回访按当时水位付全额冷税 |

### 配方 5：多会话并行 + 跨会话通讯

并行的状态纪律（板 / 案 / 双层交货件 / 一案一笔）见 02；这里只讲**会话之间怎么通话**。

**版本门槛**：claude CLI **v2.1.224（2026-08-07）**起自带跨会话消息（SendMessage / ListAgents，`/peers` 看对端；原生 Windows 需 ≥2.1.234）；**v2.1.236（2026-08-19）**起有 `notify_when_idle`。版本不够就没有这套工具，先 `claude update`。

**官方机制三句话**（出处：官方 cross-session-messaging 与 costs 文档）：

1. 消息只是一段文本，**不携带对方的历史或文件**——官方原话：要搬整个对话或上下文用 resume，别用消息。（搬状态两个都不是答案：收口换生，读案文件。）
2. 消息投给空闲会话会**开一个新 turn 并发送全量上下文**——官方把跨会话消息列为"长会话用量攀升"的成因之一。这是 02 B7 那条纪律的官方印证；"唤醒付水位 × 2、实测靶子多在 20~60 万当量"是本 kit 的实测口径。
3. 消息超限的官方报错建议原话："把大块内容放进对方能读的文件里，别塞进消息"——**收件位 / 交货件模式的官方背书**。

**用法表（B7 规则 + 新原语）**：

| 场景 | 动作 |
|---|---|
| 派活后等结果 | **不发消息催进度**。派时对执行会话订阅 `notify_when_idle`（零 token、不唤醒对方；仅主会话可订、仅本机、一次性）→ 收到空闲通知后**读交货件**，不进对方上下文 |
| 通知一个活跃的持笔会话 | 先查持笔与活跃度（B7：≤1h 活跃过才直发）；消息只放一句话 + 文件路径，大料落盘 |
| 目标无活持笔（候接手 / 已退役） | 不发信——已关会话根本不在可寻址名单里（会话须在线绑定收件通道才可达）；半死不活的别赌。往收件位追加一行 |
| 高水位讨论会话怕被打扰烧钱 | 把 `crossSessionInbound` 设为 `hold`（官方给的成本缓解），批处理收件 |

**别指望消息做的三件事**（官方边界）：替你批准权限、改配置、执行命令——收到的消息一律干不了这些，发出的消息也不携带你的授权；对方正在干活时消息只在工具调用间隙被读，不打断执行。

### 配方 6：换模型日

| 项 | 内容 |
|---|---|
| 识别 | 新模型发布，手头有多个在跑的会话想换过去 |
| 装哪层 | 至少知识层（案文件）——没案文件的会话先 `/ctx-handoff` 补一份案再换 |
| 节奏 | `/ctx-status` 点清手上有哪些案在办（各会话水位读数走周检 `/ctx-checkup`）→ 水位过线（线见 02 第 1 章）的逐个 `/ctx-handoff` 收口 → 用新模型开新会话 `/ctx-takeover C-NN` → **四层复述加抽查都过了才算换完**；水位没过线的低水位会话直接在原会话 `/model` 切了接着干，不必换窗口——**换窗口只对高水位会话划算**。当天只换在办的，已收口的案不用动 |
| 反模式 | 在高水位会话里直接 `/model` 切模型——缓存按模型隔离（出处：官方缓存文档与 Cookbook 成本优化页，原话 "caches are model-scoped"），切的那一刻整个上下文按新模型重写一遍缓存，等于付一次全水位重建费，而接手只付案文件（这笔重建费实测过一次：终端 claude CLI 用 `-p` 接续同一会话、约 5 万 token 上下文，切到新模型那一步整段重写，前一个模型建的缓存一条也没读回来。换之前先用新模型接一轮，看读回多少缓存，再决定要不要整批换）；一口气把所有会话收完口再逐个接手，中间不抽查；继任去读旧会话转录 |

## 六、故障排查

**Q：我用的是纯终端的 claude CLI，不是桌面版，有什么不一样？**
A：只差一件事——**纯终端没有会话标题工具**。`ctx-takeover` / `ctx-kickoff` 会自动跳过"设标题"这一步并在回复里说明"UI 标题未同步"，**案文件里的持笔照改**（那才是唯一的交接信号）。其余功能一律不受影响：分诊、立案、收口、接手、周检、大料下放全部照跑。

**Q：脚本报的"项目目录"不对怎么办？**
A：它按 cwd 推导（非字母数字字符全换成 `-`）。软链接、`/tmp` 这类会被解析成真实路径而对不上，直接加 `--project <目录>` 指定。

**Q：装完了但它不分诊、不提收口？**
A：条文没被加载。检查 `CLAUDE-snippet.md`（仓库根）的内容是否真进了项目 `CLAUDE.md` 或 `~/.claude/CLAUDE.md`，再跑一遍 README 的"装完自查三条"。

其余疑问（该不该用、为什么这么设计、某个说法靠不靠谱）→ **05-FAQ**。
