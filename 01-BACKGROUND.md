# 01 背景：这套纪律凭什么成立（实测报告）

**结论（一行）**：长会话的钱不漏在缓存参数上（会话内命中率 >99%，没有可调空间），**漏在生命周期上——重复重写占长寿讨论会话总花费 56~57%**；解法只有一个方向：低水位 + 连续干 + 用完即弃。**"上下文过半就变笨"本批未做任何本地测量**，只有外部定性证据，不作为本 kit 任何阈值的依据。

**七组对照，一屏看完**（详细读数见第 3 节；1~5 组同一批档案可用 `scripts/cache-audit.py` 复算，6~7 组是两次独立试点、各 n=1）：

| # | 对照 | A 组读数 | B 组读数 | 判定 |
|---|---|---|---|---|
| 1 | **热回访 \| 冷回访** | ≤15 min 回来：全热，零重写 | >1 h 回来：160 样本冷 147；税额 = 当时水位 × 2 | 冷税成立；**税基是水位，与离开多久无关** |
| 2 | **继续养着老会话 \| 收口换生** | 老会话每次冷回访 1.38M / 1.62M 当量 | 继任开题首轮 10.1 万 / 12.5 万当量 | 换生 = 老会话单次回访的 **7~8%**；两次试点沉淀完整性 4/4 |
| 3 | **不压缩接着用 \| 压缩后接着用** | 6 个请求约 47.8 万当量 | 6 个请求 28.3 万；压后隔夜回访再省 78% | 压缩**只在"还要接着用"时**回本，第 2 个请求回本 |
| 4 | **压完就走 \| 压完接着用** | 白付一次重建费（11.06 万当量） | 见对照 3 | 压完就走 = 纯亏；该做的是收口换生 |
| 5 | **调缓存参数 \| 改生命周期纪律** | 会话内命中率已 >99%，可省空间 ≈ 0 | 重复重写占总花费 56~57%，全在生命周期上 | **省钱的杠杆 100% 在纪律侧**，不在参数侧 |
| 6 | **同模型接续 \| 换模型接续** | 同模型 resume 全命中：读 51.5k / 写 55 | 换模型后第一条整段重写 ≈1.004N；同材料 resume 一轮挂牌价 $0.026 → $0.783 | 缓存按模型隔离；肥会话换模型前先收口（**n=1**，环境限定见 3.8） |
| 7 | **整读案文件 \| 渐进加载** | 整读口径接手加载 89,169 字符 | 渐进加载 14,806 字符（降 83%），活跃行零遗漏 | 入场税基口径由"文件多大"改为"接手实际加载量"（**n=1**，见 3.9） |

---

## 1 测试目标：这批测量要回答什么

先写清要回答的问题，再谈过程。每问都有预先写死的读数口径。

| # | 要回答的问题 | 用什么读数回答 | 本批状态 |
|---|---|---|---|
| Q1 | 长会话的钱到底花在哪？会话内缓存还有没有优化空间？ | 各类花费占总当量的比例；会话内缓存命中率 | **已测** |
| Q2 | 离开多久回来会"冷"？冷一次要付多少？ | 停摆间隔分档的冷/热计数；冷回访的写/读字节 | **已测** |
| Q3 | compact（压缩）划不划算？什么条件下回本？ | 压缩前后水位、重建首请求当量、回本点 | **已测** |
| Q4 | 收口换生划不划算？状态真沉淀得下来吗？ | 继任开题首轮当量（门限 ≤15 万）；3+1 抽查通过数；老会话真退役与否 | **已测（两波试点）** |
| Q5 | "上下文用量过半，模型判断力下降"在本地站得住吗？ | 需要固定题库 + 盲评的能力对照实验 | **未测**，见第 5 节栏二 |

**Q5 必须单独看一眼**：本 kit 里所有水位线（150k 黄、200k 红）**全部是按钱定的**（重写税基），不是按质量定的。引用时别把两件事串成一件。

## 2 方法与口径

**审计法**：读会话 jsonl 里每条 assistant 行的 `usage` 四个字段（input / cache_read / cache_creation / output），按下式折算成统一单位。配套脚本 `scripts/cache-audit.py`。

```
当量 = input×1 + cache_read×0.1 + cache_write×2 + output×5
```

**样本**：主样本 = 两个长寿讨论会话（原始 704 条请求记录行，跨多天）；旁证 = 同项目 119 份转录抽查 + 4 件 compact 标本 + 2 波收口换生实弹试点。采集环境：claude CLI · 1M 上下文窗口 · Opus/Fable 档 · 2026-08。

**两把尺，别混用**：704 是**原始请求级记录行数**（同一条回复的多行未去重）；下文"约 150 / 190 条"是**去重后的主链请求数**（按 message id 去重、剔除子代理行）。同一批档案两把尺各自复核过：原始 719 行 / 去重 348 条，差额全是同 id 多行。

**判负纪律**：读数不达标照实写。本节所有数字均可用上述脚本对同一批 jsonl 复算。

---

## 3 栏一 · 本批实测（每条带证据）

### 3.1 计价机制三条

| # | 结论 | 证据 |
|---|---|---|
| 1 | 缓存写入 **100% 走 1 小时桶，写价 2×**（不是 5 分钟桶 / 1.25×） | 两个长寿会话共 **704 条请求，5 分钟桶写入 0 字节** |
| 2 | 系统头 **~24k 跨会话共热**（同模型下字节数相同），正文只能由本会话自己的请求续温 | 所有冷回访恒读 ~24k，与会话身份无关 |
| 3 | **子代理不给主会话续 TTL** | 机制：子代理跑在自己的上下文里，请求不触碰主会话前缀；实测：119 份转录中主会话账上 0 条子代理请求（子代理行独立计账） |

一段 token 一生只有三个价：**入场 2×（付一次）→ 驻留 0.1×／轮 → 过期重入再付 2×（税基=当时水位）**。后面所有对照都是这三个价的推论。

### 3.2 对照 1：热回访 | 冷回访

| 停摆间隔 | 判定 | 样本 |
|---|---|---|
| ≤15 min | **稳热** | 多组连击全热零重写（含一组 0.3~9.8 min 六连击） |
| 15–60 min | **碰运气** | 28 样本中冷 8（29%） |
| >1 h | **必冷** | 160 样本中冷 147、踩线热 13（均落在 1 小时刚过的边界带） |

冷签名（一眼认）：**写 ≈ 全量水位、读 ≈ 24k 共享头**。看到这个组合就是整段冷重写。

**冷税三件套标本**（同一批档案，三个不同水位）：

| 标本 | 离开时长 | 回来时水位 | 实测写/读 | 冷税当量 |
|---|---|---|---|---|
| 本会话（压缩后） | 64 min | 117k | 写 117,660 / 读 24,248 | **0.24M** |
| 会话 B | 5 天 | 711k | 写 687,018 / 读 24,183 | **1.38M** |
| 会话 A | 5 天 | 830k | 写 805,907 / 读 24,183 | **1.62M** |

**64 分钟 vs 5 天，时长差 100 倍，税额只跟水位走**（0.24M vs 1.38M/1.62M，比值就是水位比值）。推论：冷税一次性、付完即回热；**离开本身免费**，钱只在回来发第一条请求时产生。

### 3.3 对照 5：调缓存参数 | 改生命周期纪律

- 两个长寿讨论会话（约 150 / 190 条请求，跨多天），**重复重写占总花费 56% / 57%**——同一段内容被整段重写 **9~13 遍**。
- 会话内缓存命中率 >99%：**没有任何会话内的缓存技巧能省这笔钱**，它漏在会话生命周期上。

这一组是整个 kit 的立论点：**杠杆不在参数侧，在纪律侧。**

### 3.4 对照 2：继续养着老会话 | 收口换生

**收口那一轮要花多少（公式，两波实测回代）**：

```
收口轮当量 ≈ 水位 × (2 + 0.1×(N−1)) + 输出 × 5     （N = 该轮请求数）
```

多请求轮里每个请求都再付一遍 0.1× 读税，占比约 20%，不可忽略。

| 标本 | 实测 | 公式 | 偏差 |
|---|---|---|---|
| 会话 A 收口 | 2.02M | 2.13M | −5% |
| 会话 B 收口 | 1.80M | 1.82M | −1.2% |

用法：收口前先按公式估出这一次的"买断价"（一次性付清的花费），与"继续养着每次回访 1.38~1.62M"对账，再决定收不收。

**两波实弹试点记分**：

| 判据 | 试点一（源=会话 A） | 试点二（源=会话 B） |
|---|---|---|
| 沉淀完整性（3+1 抽查） | 4/4，负对照零编造、按指针定点取 | 4/4，负对照定点取证并自认待补落盘项 |
| 继任开题首轮当量（门限 ≤15 万） | **12.5 万** | **10.1 万** |
| 未落盘清单如实 | 7 项 + 主动补落盘 1 件 | 14 项（含自报 3 处账错、2 个不可复现数字） |
| 模板可用 | v1 跑通，暴露出 3 处要修的地方 | **v2 零修正** |
| 老会话真退役 | 初验 0 请求 | 初验 0 请求 |
| 收口买断价 vs 公式 | 2.02M（−5%） | 1.80M（−1.2%） |
| 状态体积 | 830k 水位 → ~14KB 案文件 | 711k → ~14KB 案文件 |

换算：**继任开题首轮 = 老会话单次冷回访的 7~8%**（12.5 万 ÷ 1.62M = 7.7%；10.1 万 ÷ 1.38M = 7.3%）；此后继任每轮跑在 ~7 万水位而不是 83 万 / 71 万，驻留税基降到 **8~10%**。

### 3.5 对照 3/4：压缩到底划不划算

**手动压缩全程实录（一次完整标本）**：

| 项 | 读数 |
|---|---|
| 压缩前后水位 | 581k → **14.7k** |
| 压缩后重建首请求 | **11.06 万当量**（读 29k 热头 + 写 44k） |
| 每请求读成本 | 5.76 万 → **0.74~0.88 万（÷7）** |
| 回本点 | **第 2 个请求** |
| 压缩后 6 个请求合计 | 28.3 万（不压情形 ~47.8 万，净省 ~19.5 万） |

**压缩后冷回访实录**：同一会话压缩后停摆 64 分钟再入，写 117,660 / 读 24,248，全轮 26.6 万当量；对照未压缩情形（水位 581k×2 ≈ 120 万），**省 78%**。

前提写死：**压缩只有在"会话还要接着用"时才回本**；压完就走等于白付一次重建费（11.06 万）——那种情形该做的是收口换生（对照 2）。

### 3.6 auto-compact 实录与 compact 的账面盲区

- auto-compact 标本：**999k 触发、耗时 198 秒、丢 98.8%**；摘要与界标**追加**进同一 jsonl，旧行全保留；压后首条重建 ~61k。
- **compact 的摘要请求不落账在 jsonl，自动与手动皆然**（3 个 auto 标本 + 1 次 manual 实测，共 4 标本）。转录审计法与基于转录的用量统计工具对 compact 成本**双盲，本地不可测**——本 kit 所有账单均不含这笔，如实标注。

### 3.7 审计法注记（踩过的坑，直接抄）

- **jsonl 时间戳是 UTC**。与本地钟表比对前先按本地时区换算——本批曾据此把"9.8 分钟"误判成"8 小时停摆"，结论方向整个反了。
- **重写判据必须带读量塌缩条件**：`单次写 >150k` 会把"大块新料首次入场"误报成重写；加上 `本次读 < 上一条上下文 ×0.5` 才是整段冷重写。修正前后：占比从 ~60% 收到 56~57%（结论方向不变，数字须用修正值）。
- **别用 `ls -t` 自查"我是哪个 jsonl"**：实测会指向两周前的旧文件。会话档案路径由验收方按账单时间戳配对回填。

### 3.8 对照 6：同模型接续 | 换模型接续（切模型缓存实测）

**采集环境（读数只在这个环境里有效）**：claude CLI 2.1.252 与 2.1.258 的 `-p --resume` 路径，同一台机、同一份约 2.7 万 token 材料、五步在秒级间隔内跑完，2026-09-02，**n=1**。判据运行前落盘，跑完不改。

| 臂 | 接续那一轮的写 / 读 | 相对原水位 N |
|---|---|---|
| 同模型 resume（`claude-opus-5`） | 写 **55** / 读 51,524 | 全命中 |
| **换模型 resume**（`claude-opus-5` → `claude-fable-5-1`） | 写 **38,990** | **≈1.004N：整个上下文按新模型重写了一遍** |
| 对照臂 · 同模型 resume（`claude-sonnet-5`） | 写 87 / 读 62,829 | 命中 **1.416N** |
| 对照臂 · 同模型 resume（`claude-fable-5`） | 写 55 / 读 51,972 | 命中 **1.241N** |

（命中值 >1，是因为 resume 那一轮读回的前缀里还含建会话那一轮就已在缓存里的系统头，口径与 3.1 第 2 条一致。）

**三条判定**：

1. **缓存按模型隔离**——换过去以后读不回旧模型建的条目，实测支持。
2. **整段重写"至少一次"**——换模型后的第一条请求把整段上下文重写了一遍（1.004N）。只能说"至少一次"，因为第二次的读数被下面那个混杂因素挡住了。
3. **"切换专属成本是多少"不可判**——预注册判据三档（通过 / 判负 / 不可判）全未命中，落在判据空隙里。挡路的是一个混杂因素：`claude-fable-5-1` 这一个模型 ID 在上述路径上**每一轮都整份重写**，与是否切换过模型无关——它的 `cache_read` 从头到尾只有一块固定的系统前缀（2.1.252 上 0.309N、2.1.258 上 0.322N，且不随材料从几百 token 涨到 2.7 万 token 而变），自己写进去的条目一条都读不回来。把 CLI 从 2.1.252 升到 2.1.258、该 ID 已进模型表之后，症状原封不动。

**这个异常的限定必须跟着一起写**：它只出现在**这一个模型 ID + 这一条 `-p --resume` 路径**上。同一台机、同一份材料、相隔几分钟：只差 ID 后缀的 `claude-fable-5` 干净命中，`claude-sonnet-5` 也干净命中；同一个 ID 在桌面客户端里同样正常（一个 89 轮的 `claude-fable-5-1` 会话 `cache_read` 占 92.1%，一个 389 轮的 `claude-fable-5` 会话占 94.1%（两项读数来自本项目两个桌面客户端会话的转录账目，2026-09-02 自查；原始转录不随仓发布，同 §2 口径））。**不得写成"换模型"的属性，也不得写成某个模型家族的属性。** 具体机制未验证，不进结论。

代价有多大，看同一份材料 resume 一轮的挂牌价：

| 模型 | resume 一轮 | 相对最便宜的 |
|---|---:|---:|
| `claude-sonnet-5` | $0.013 | 1× |
| `claude-opus-5` | $0.026 | 2× |
| `claude-fable-5` | $0.053 | 4.1× |
| `claude-fable-5-1` | **$0.783** | **60×** |

**能怎么用**：肥会话换模型之前先收口；真要在原会话里换，换完先看一眼那一轮的 `cache_read`——读不回来就是缓存没建起来，此后每一轮都在按整份水位付钱。

### 3.9 对照 7：整读案文件 | 渐进加载

样本是另一个项目里一个**跑了约一个月的真实案**（不是本仓的案，也不是上面测量用的那批会话）：测量那一刻全文 106,618 字符（该案仍在增长，前一日量得 104,030），其中实验台账一节就占约 7.4 万字符、96 行。**n=1**，全程只读。

| 接手时的读法 | 该案加载量 | 对照案（本仓自己的一个在跑的案） |
|---|---|---|
| **整读口径**（头行 + A~E 全文 + 收件位） | **89,169** 字符 | 15,405 |
| **渐进加载**（头行 + A~D + 实验台账的表头与活跃行与最近一条已交货判定 + 收件位） | **14,806**（降 **83%**） | **12,654** |

**活跃行零遗漏**：改后读法取到的活跃行，与逐行比对状态列的结果一致，一行不漏（对照案 6 行活跃全部取到）。

**判定**：入场税基的口径要从"文件多大"改成"接手一次实际加载多少"。同一份文件两种读法差 6 倍，差额几乎全在实验台账那 96 行上——旧口径只量到台账为止，于是台账不限长又没人管，而接手时真正付钱的正是它。尺寸线口径随之改（见 02-METHOD 2.6）。

---

## 4 过程注记（可跳过）

本批测量的顺序：先扫全量转录做冷热分档 → 发现冷签名 → 回头量冷税税基（三件套）→ 做压缩标本 → 两波收口换生实弹试点。中途返工两次，都记在 3.7：一次是时区误判导致分档结论方向反了，一次是重写判据太松导致占比虚高（~60% → 56~57%）。**两次返工都是靠换一把尺重算发现的，不是靠复读同一份数据。**

---

## 5 栏二 · 经验规则，未实测

本栏的任何一条都**不得当作栏一使用**。每条写明状态与建议测法。

### 5.1 "上下文用到 50~60% 就变笨"——按证据强度分三档

**这句话本 kit 不采信为阈值依据**，因为三档证据说的不是同一件事：

| 档 | 能引什么 | 关键限制 |
|---|---|---|
| **一档：厂商官方，仅定性** | Context windows 文档原句："As token count grows, accuracy and recall degrade, a phenomenon known as context rot."（token 越多，准确率与召回越差，官方把这个现象命名为 context rot）<br>https://platform.claude.com/docs/en/build-with-claude/context-windows<br>工程博客同一现象的机制展开："as the number of tokens in the context window increases, the model's ability to accurately recall information from that context decreases"<br>https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents<br>客户端文档 "When context fills up"：接近上限时早期的详细指令可能丢失<br>https://code.claude.com/docs/en/how-claude-code-works | **官方全文没有任何百分比阈值**。只说"越长越差"，没说"过了 X% 就变笨"。 |
| **二档：三方研究，量纲是绝对长度或信息位置** | Chroma《Context Rot》（2025-07）：18 个前沿模型 8 档长度全部随输入变长退化，1M 窗口模型在 50K token 已现退化 https://www.trychroma.com/research/context-rot<br>Lost in the Middle（Liu et al., TACL 2024）：相关信息处在中段时，准确率比处在首尾**低 30 多个百分点**（U 型）https://aclanthology.org/2024.tacl-1.9/<br>NVIDIA RULER（2024）：模型"有效长度"普遍小于宣称窗口（某模型 4K 时 96.6% → 128K 时 81.2%）https://github.com/NVIDIA/RULER<br>NoLiMa（ICML 2025）：32K 长度时 12 个模型中 10 个跌破短上下文基线的 50% https://arxiv.org/abs/2502.05167 | **量纲是绝对 token 数与信息位置，不是窗口占比**，不可换算。NoLiMa 那个"50%"指的是"相对自身短上下文基线打对折"，**不等于"用到窗口 50% 就变笨"**。 |
| **三档：社区经验法则，归因不可证** | "用量到 50~60% 时主动 compact，理由是摘要基于完整信息生成才准" | 属于操作建议，不是实测结论；常见的归因（说某位工程师推荐）**查不到一手出处**；还有博客自称"这是厂商自家工程数据"但给不出可验证引用，属冒用背书，不可采信。 |

**能怎么写、不能怎么写**：可以写"官方承认 context rot：token 越多准确率与召回越差（官方未给百分比）"；可以写"三方研究支持越长越差，量纲是绝对长度"；**不能写**"官方认为 50~60% 后判断力下降"。

**另一件常被混为一谈的事**：客户端的 auto-compact 默认阈值在窗口顶部（1M 窗口约 967K ≈ 96.7%，200K 窗口在 200K 边界压缩，见 https://code.claude.com/docs/en/model-config ）。那是"系统何时替你压缩"的**工程参数**，不是"判断力何时下降"的性能拐点，**两件事分开讲**。顺带作为一条反证信号：若官方真认定 50~60% 是性能拐点，默认压缩阈值不会设在接近满窗处。

**本批为什么没测**：需要能力基准对照实验。建议测法：备一套有客观答案的固定题（N≥20），A 组在 <20% 窗口水位跑，B 组用与题无关的填充料把水位垫到 50~60% 后跑同一批题，比正确率与指令遵从率，判分者不知组别。

### 5.2 其余三条

| 说法 | 状态 | 处理 |
|---|---|---|
| "15–60 分钟这一档大概三成会冷" | **样本 28 偏小** | 冷 8/28 = 29%，正态近似 95% 置信区间约 [12%, 45%]——只够支撑"别赌"，**不够定精确阈值**。要定阈值须把这一档样本加到三位数 |
| ">1h 必冷" 里的 13 例踩线热 | 未逐条复核 | 均落在 1 小时刚过的边界带，按噪声处理；不改变"1 小时后按必冷做计划"的操作口径 |
| "压缩比收口换生省" | 未对照实测 | 本批只测了"压缩 vs 不压缩继续用"（省 78%），**没测"压缩 vs 换生"**。两者定位不同：压缩保上下文、丢细节；换生保状态、丢上下文。默认按 3.5 的前提判——压完还要接着用才压 |

---

## 6 适用边界与已知盲区

- **采集环境**：claude CLI + 1M 上下文窗口 + Opus/Fable 档，2026-08。主样本=两个长寿讨论会话（704 请求）；旁证=同项目 119 份转录抽查 + 4 件 compact 标本。
- **换栈必须重测**：不同模型、不同窗口尺寸、API 直连或其他客户端，缓存实现可能不同。要重测的就三个数——**TTL 桶时长、写价倍率、共享头字节数**；跑 `scripts/cache-audit.py --all` 看自己的账，再决定阈值。
- **已知盲区**：① compact 成本不落账，本地不可测；② 转录审计只见落账请求；③ 子代理成本单独计账，不进主会话行（要单算得另扫子代理行）。
- **样本外**：本批全部数据来自讨论型与执行型两类会话，没有覆盖长时间运行的自动化流水线型会话。
- **对照 6 与对照 7 两项均 n=1**：各只跑过一次（判据均预注册、跑完不改），措辞只到"实走过一次"，不支撑比率与"已验证通用"。对照 6 的异常还绑定在具体模型 ID 与 `-p --resume` 这条路径上，对照 7 的样本是单个真实案，换环境、换案都须重测。
- **未测**：Q5（质量随水位退化）本地零测量，见 5.1。
