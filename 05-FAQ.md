# 05 释疑

**这本册子怎么用**：一条一个疑问，用提问者的原话开头，答案两三段说完。正文（02-METHOD）只讲"是什么、怎么做"，"我不服 / 我有疑问"全部收在这里，两边不混排。找不到你的问题就照第 1 类的写法自己提一条——本册按被真实问到的顺序增补。

分四类：**① 该不该用 ② 为什么这么设计 ③ 具体操作 ④ 某个说法靠不靠谱**。

---

## 一、该不该用

### Q1 "我们就是个普通功能开发项目，是不是不用这套？"

**经济层要用，知识层多半不用。** 别从"功能开发有 ticket 和 PR"直接跳到"整套都不需要"——功能开发里有三块研究型工作，照样存在：

1. **遗留代码考古**——"这坨老代码到底怎么跑的"：大料下放（digest 子代理）的主场。
2. **跨需求的架构决策**——影响多个需求、要权衡的选型：一个小案或一条决策记录。
3. **超过一天的排障追凶**——已经进入"假设 → 实验 → 排除"循环，值得立案。

对传统功能开发项目，价值排序：**经济纪律 > 大料下放 > 把 ticket / PR 写成自足交接件的习惯 > 案机制。** 前两条人人受益（那是计费物理），最后一条按需启用。

### Q2 "为什么'一人一需求'在传统开发里行得通，研究型工作就不行？"

**不是因为功能开发简单，是因为它的状态载体成熟。** 功能开发早就有自己的一套"案机制"，只是没人这么叫它：

| 案文件的哪一节 | 传统功能开发里的对应物 |
|---|---|
| A 目标 | ticket / 需求单 |
| B 当前方案快照 | 设计文档 / PR 描述 |
| C 已拍决策 | 决策记录（ADR） |
| E 实验台账 | 测试用例与跑果 |
| F 编年志 | git log |
| 验收 | code review |

新人接手读 ticket + PR 描述 + 测试就能上手，不必去翻上一个人的聊天记录——**这就是"状态在盘上"，只不过那个盘是行业标配的，成熟到没人意识到它的存在。**

研究型工作没有这套。它的状态长这样：「这条路试过三轮都不到门限、病根是 X 而不是阈值、别再试 Y」。这种东西 ticket 装不下（它不是任务）、PR 装不下（没代码）、决策记录装不下（还没到能拍的程度）。**没有行业载体收留，它就只能住在会话里；会话一死，状态归零，下一个人从头再试一遍那三轮。** 案文件的本质，就是把科研的**实验记录本**引进 AI 会话工作流：假设、试法、读数、判定、下一步，一条一行。

### Q3 "我们做的是同一个产品，为什么有时候要立案、有时候又不用？"

**因为"要不要立案"跟的是工作块的类型，不是产品的类型；同一个产品在不同阶段会落在不同类型上：**

- 研究段定型之后，活退化成批量执行——**知识层需求骤降**，案文件从"每天更"变成"归档留证"。
- 普通项目撞上疑难（一个 bug 追了三天、一个选型吵不出结果），**临时进入研究态**，该立案就立案。

所以不必替项目"选一层"装：分诊（立案 / 散活 / 顺手改）本来就在工作块粒度上逐次选，每次说事都重新判一遍。

### Q4 "不装这套工具，纯手工能不能做到？"

**能，原则先于工具存在。** 水位过线该换生、冷回来第一句直接说正事、大料不进主上下文——这些是计费物理的推论，你手工执行同样有效。skill 不发明任何原则，只是把每条原则压成一条命令，省掉"想起来要做"和"记得怎么做"这两步。

反过来也成立：**只装工具、不认原则，就只是走个形式**——它会照常建案文件，而你照常在一个 800k 的会话里聊到天亮。

---

## 二、为什么这么设计

### Q5 "为什么账要按'活'记，不按会话记？"

**因为会话是可抛弃的执行载体，而可抛弃者不得作账本主键。**

执行载体有两种：会话（session）与子代理任务（task）。它们和"活"之间是多对多的：除了一对一，还有**一个会话顺序跑多个活**，和**一个活跨两个会话**（换生）。任选一种载体作主键，这两种形态立刻把记录撕成两半。

所以主键必须是活（案 / 实验），载体只出现在两个地方：案头行的"持笔 / 载体"字段，和 G 节的档案指针。换生时 **E 行零改动，只改头行署名**——账本唯一、主键恒定。

### Q6 "那给每个会话建一份摘要文档不行吗？"

这是最常见的直觉方案，它有三个必然结局：

1. 换生把**一个活**的记录撕进**多份**摘要；
2. 摘要与任务台账双写，必然漂移；
3. 会话死后摘要冻结，"当前状态"无处安放。

病根同一个：拿载体当主键（Q5）。正确分工是——**状态按活组织（案），过程按载体归档（jsonl），查询靠投影（会话登记簿 / 任务台账）**，三者不混。

### Q7 "'进场/在场/退场'那张图必须存在吗？"

**不是必须，它是速查卡不是流程关卡。** 那张图是这套纪律最早的形态，现在的正式形态是第 1 章那十条带门限的原则；图只是把十条压成一屏，方便你在发消息前扫一眼。

判断标准很简单：如果你已经能张口说出"冷税税基是水位""高水位不派子代理"，图对你就是冗余；如果还要想一下，就留着它。**它没有独立信息量**——图里每一格都能在第 1 章找到对应的原则编号，这是有意为之，防止两处规则各自演化。

### Q8 "为什么执行体不许读案全文？"

三个理由，**第三个最重**：

1. **省钱**：案全文进上下文，每轮都要付驻留税；
2. **冻结口径**：任务书里抄进去的那段是执行时的唯一口径，防它读到讨论里的中间态；
3. **防独立性污染**：执行体读了讨论上下文，它就会沿着讨论方的思路走一遍，然后得出"果然如此"——那不是独立验证，是**假确认**。要它验的东西，越不知道你希望它验成什么样越好。

同理，"验证"和"执行"最好也不是同一个会话；实在同一个，至少判据要在动手前写死（预注册），事后不许改。

### Q9 "为什么非要边讨论边落盘？收口的时候一起写不行吗？"

**因为案文件的价值不在收口那一刻，在它让任何时刻弃置会话都不亏。**

等收口才写有两个问题：一是收口前的每一分钟，你都欠着一笔"会话一崩就全没了"的债；二是**收口会变成大工程**——如果收口时才发现要写的东西很多，那正说明前面漏了。正确状态下收口只是把零散尾巴归位 + 列一张未落盘清单，几分钟的事。

判据一句话：**此刻关掉这个会话，明天用一个 ≤10,000 字符的文件能不能接着干？**

---

## 三、具体操作

### Q10 "什么时候该 compact（压缩）？"

**只有一种情形**——执行会话逼近上下文窗口顶、来不及落盘时的急救。讨论 / 评审型会话**永不 compact**。压缩保上下文、丢细节（实测一次自动压缩丢 98.8%）；你真正需要的是保状态，那是收口换生干的活。

### Q11 "会话变慢变贵了，压一下清爽点？"

压缩要回本，前提是**压完还接着用**（实测第 2 个请求回本）。压完就走 = 白付一次重建费（实测 11.06 万当量）。压完还要接着聊很多轮才划算——但那种情形通常说明你该收口了。

### Q12 "我要离开一晚上，走之前要不要先压一下？"

不用。**离开是免费的**，钱只在你回来发第一条请求时产生。走之前唯一该做的是确认结论落盘了；水位真高了就顺手收口，明天开新会话接手（全免冷税）。

### Q13 "收口是不是很麻烦，要写一大堆？"

**边讨论边落盘**的话，收口只是扫尾，两分钟。如果收口时发现要写的东西一大堆，说明前面漏落盘了——这本身就是个信号（见 Q9）。

### Q14 "这活派子代理还是开新会话？"

三问：结果 ≤15 分钟出且当场要用 → 子代理；跑 >1 小时或派完你就走 → 新会话；N 臂要当场横比 → 一次性低水位编排会话。另外记住：**高水位会话不派子代理**——等它回来的冷税 = 派发者当时水位 × 2。

### Q15 "能同时开两个会话接同一个案吗？"

不能。一案同一时刻**一支笔**，双接必然对不上账（两边各改 B/C，后写的覆盖先写的）。接手时如果"持笔"栏已有别人，它会先警告。并行要拆成两个案，或者拆成 E 行派给执行体（执行体只写自己那行 E，天然不冲突）。

### Q16 "案文件越写越长怎么办？"

硬线是**头行 + A~D ≤10,000 字符为绿、>15,000 必瘦**（2026-08-24 放宽，原 7,000；这是每个新进场者的入场税基）。超了首选把 C 表旧行迁存本线决策档案，再把方案正文升格成独立编号文档，B 节只留一张顶层图 + 指针。E/F/G/H 不限长，继任不必整读。

### Q17 "周检看到某个会话重写占比 40%，很严重吗？"

意思是这个会话四成的钱花在"把同一段内容重新写进缓存"上。判据线是 <10%。解法只有一个：收口换生。**不是任何缓存设置能救的**——会话内缓存命中率本来就 >99%。

---

## 四、某个说法靠不靠谱

### Q18 "'上下文用到 50~60% 就变笨'，这说法靠谱吗？"

**方向对，那个数字没有一手出处，而且量纲被换错了。** 三档分开看（完整引文与链接见 01-BACKGROUND 第 5.1 节）：

- **官方口径（仅定性）**：厂商文档承认 context rot——"token 越多，准确率与召回越差"。**全文没有任何百分比阈值。**
- **三方研究（量纲是绝对长度 / 信息位置）**：多项研究支持"越长越差"，但读数都是按绝对 token 数或信息在上下文中的位置给的，**不是窗口占比**，不能换算。其中一项的"跌破基线 50%"指的是"相对自身短上下文基线打对折"，不等于"用到窗口 50% 就变笨"。
- **社区经验法则**：确实有人建议用量到 50~60% 就主动压缩，但那是操作建议不是实测结论，常见的归因查不到一手来源。

**所以本 kit 的处理是**：承认方向（越长越差），不采信那个数字作阈值，也不把三方结论写成官方口径。

### Q19 "那 150k / 200k 这两条线是怎么定的？"

**按钱定的，不是按质量定的。** 它们来自重写税基：水位越高，一次冷回访、一次收口、一次子代理返回的花费就越高，150k / 200k 是"再往上就不划算"的经济线。质量维度本批**零本地测量**（见 Q18），两件事别串在一起讲。

要按质量定线，得做能力对照实验：一套有客观答案的固定题（N≥20），一组在低水位跑、一组用无关填充料把水位垫到目标区间跑，比正确率与指令遵从率，判分者不知组别。谁做了这个实验，欢迎把读数拿来改这一节。

### Q20 "自动压缩阈值都设在 96.7% 了，是不是说明用到 90% 也没事？"

**不能这么推，那是两件事。** 自动压缩阈值是"系统何时替你压缩"的**工程参数**（1M 窗口约 967K，200K 窗口在边界压），不是"判断力何时下降"的性能拐点。它能说明的只有一件事：**官方并没有把 50~60% 当成性能拐点**，否则默认值不会设在接近满窗处。

它同样不能反过来当"用到 90% 也没事"的背书——退化是连续的，没有拐点不等于没有退化。本 kit 的建议仍然是按钱走：到线收口，别养会话。

### Q21 "这些数字换个模型 / 换个客户端还成立吗？"

**机制大概率成立，数字必须重测。** 要重测的就三个数：**TTL 桶时长、写价倍率、共享头字节数**。跑 `scripts/cache-audit.py --all` 看自己的账，再决定阈值。已知盲区也一并看 01-BACKGROUND 第 6 节：压缩自身的成本不落账，本地审计看不见；子代理成本单独计账，不进主会话行。
